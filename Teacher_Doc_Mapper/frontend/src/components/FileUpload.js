import React, { Component } from "react";

export default class FileUpload extends Component{
    constructor(props){
        super(props);
    }

    render(){
        return <p>This is the file upload page</p>
    }
}